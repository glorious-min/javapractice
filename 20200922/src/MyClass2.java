
public class MyClass2 {
 int age; // 왜 프라이빗으로 바꾸어도 객체에서 출력이 되는거지 ?? 

	
	//MyClass2의 생성자 
	//클래스의 이름과 동일한 이름을 가지며 반환타입은 존재하지 않는다 
	//생성자가 호출될떄 전달받은 20은 newAge에 파라미터로 전달된다 
public  MyClass2(int newAge) {
age =newAge;
}
}
